/**
 * Permitir el ingreso de varios AB y determinar cuáles,
 * entre ellos son SIMILARES (iguales estructuras, contenidos
 * de los nodos distintas), EQUIVALENTES (similares, y los nodos
 * contienen los mismos valores) y DISTINTOS (con estructuras distintas)
 * Autor: Andres Gaibor
 */

#include <iostream>
#include "../utilidades/arbolBinario.h"

using namespace std;

int main()
{

    return 0;
}