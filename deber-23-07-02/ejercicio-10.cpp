/**
 * Permitir el ingreso de varios AB y determinar cuáles,
 * entre ellos son LLENOS (no hay nodos que tengan un solo nodo descendiente),
 * COMPLETOS (hasta el nivel n-1 todos los nodos son llenos, y en el nivel n
 * todos los nodos hoja ocupan las posiciones mas a la izquierda) y DEGENERADOS
 * (solo existe un nodo hoja, y todos los demás nodos solo tienen un nodo descendiente)
 * Autor: Andres Gaibor
 */

#include <iostream>
#include "../utilidades/arbolBinario.h"

using namespace std;

int main()
{

    return 0;
}